---

---

<div class="text-primary content">
  <slot />
</div>

<style lang="scss">
  .content {
    display: flex;
    flex-direction: column;
    font-size: var(--fs-m);
    padding-top: var(--space-96-120);
    max-width: 960px;
    width: 100%;
  }
</style>
